# API Guidelines

- Prefer explicit request and response examples.
- Document error cases with status codes.
- Call out authentication, rate limits, and retries.
- Keep payload field names stable once shipped.

Use this reference when drafting API-oriented work.
